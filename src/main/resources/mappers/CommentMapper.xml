<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="Comment">
    <select id="commentCount" resultType="int">
        SELECT
            COUNT(*)
        FROM COMMENT
    </select>
    
    <select id="commentList" resultType="com.bu.bumoim.domain.Comment">
        SELECT
            *
        FROM COMMENT
    </select>
    
    
    <insert id="commentInsert" parameterType="com.bu.bumoim.domain.Comment">
        INSERT INTO
        COMMENT (COMMENT_NUMBER, photo_number ,comment_writer , COMMENT_CONTENT, COMMENT_DATE)
        VALUES(#{comment_number},#{photo_number},#{comment_id},#{comment_content},CURDATE()) 
    </insert>

	 
    <update id="commentUpdate" parameterType="com.bu.bumoim.domain.Comment">
        UPDATE COMMENT
            SET
            CONTENT = #{content}
        WHERE COMMENT_NUMBER = #{comment_number}
    </update>
    
    <delete id="commentDelete" parameterType="int">
        DELETE FROM COMMENT WHERE COMMENT_NUMBER = #{comment_number}
    </delete>


<!-- Gallery -->

	<insert id="galCommentInsert" parameterType="Comment">
        INSERT INTO
        comment (comment_number, photo_number ,comment_writer , comment_content, comment_date)
        VALUES (#{comment_number}, #{photo_number}, #{comment_id}, #{comment_content}, CURDATE()) 
    </insert>    

	<select id="galCommentList" parameterType="int" resultType="Comment">
        SELECT
            comment_number, photo_number, comment_writer as comment_id, comment_content, comment_date
        FROM comment 
        WHERE photo_number = #{photo_number}
    </select>
    
    <delete id="galCommentDelete" parameterType="int">
        DELETE FROM comment WHERE photo_number = #{photo_number}
    </delete>
</mapper>